{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "php artisan config:cache && php artisan route:cache && php artisan view:cache && php artisan migrate:fresh --force && php artisan serve --host=0.0.0.0 --port=$PORT",
    "healthcheckPath": "",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
